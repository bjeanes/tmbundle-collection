To add new bundles to this list use the `submodules` command:

  git submodule add git://github.com/user/project-tmbundle.git "Project.tmbundle"
  git submodule update --init
  git commit -m "Added project-tmbundle"
  git push
